#pragma once


enum GenerationModes {
    rowGeneration, columnGeneration, blockGeneration
};


enum DifficultyLevel {
    Easy, Medium, Hard, Expert, Master, Extreme
};


enum ButtonActions {
    saveGame, loadGame, undo, hint, erase, newGame, exitGame, sketchMode, numeric1, numeric2, numeric3, numeric4, numeric5, numeric6, numeric7, numeric8, numeric9
};